# Task Plot Review

## Round 1 — Revise

- Evidence match: phase order, row types, response keys, and 2,000/2,400 ms timings are correct.
- Readability: pass; labels are legible and screen/timing elements do not overlap.
- Scale/layout: pass; all three rows use consistent screen boxes and proportional bars.
- Stimulus accuracy: revise. The `KEEP GOAL` and `SWITCH GOAL` preview screens show a blue `CURRENT` progress bar but omit the current seafood icon beside that bar. This makes same-good versus different-good selection visually ambiguous.
- Targeted revision: add a small crab icon beside the `CURRENT` progress bar in both preview screens; keep the `KEEP GOAL` result as crab, and keep the `SWITCH GOAL` result as fish. Preserve all other structure, wording, timings, and the blank header.

## Round 2 — Pass

- Evidence match: pass. Exactly three representative trial types are present, and phase order matches the canonical task.
- Stimulus accuracy: pass. Each preview contains crab, octopus, and fish offers; current-net crab icons now make persistence versus switching explicit; green/red offers and blue progress bars are correct.
- Timing/response labels: pass. `until SPACE`, `2,000 ms`, `1 / 2 / 3`, `30 s max`, and `2,400 ms` are legible and attached to the correct phases.
- Layout/scale: pass. No overlap, clipping, incorrect aspect ratio, inconsistent screen scale, or excessive whitespace.
- Branding: pass. The generated raw image keeps the header clear; post-processing adds a centered title, centered `Construct:` subtitle, and borderless top-right TaskBeacon lockup without overlap.
- Unsupported content: none. No scanner phases, fixation, people, devices, decorative scenery, extra rewards, or model-generated branding appear.
- Final result: accepted after 2 rounds (1 targeted regeneration).
